import phones from './mockPhones';

export const fetchPhones = async () => new Promise((resolve) => {
  resolve(phones);
});

export const loadMorePhones = async ({ offset }) => new Promise((resolve) => {
  resolve(phones);
});
